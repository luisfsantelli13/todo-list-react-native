// Troque pelo endereço onde sua API está rodando.
//
// - Se for testar no emulador Android: use http://10.0.2.2:3000
// - Se for testar no celular físico (Expo Go): use o IP da sua máquina na rede,
//   ex: http://192.168.0.10:3000 (rode "ipconfig getifaddr en0" pra descobrir)
// - Se já tiver feito deploy do backend (Render, Railway, etc): use a URL pública

export const API_URL = 'http://192.168.15.106:3000/api/tasks';
